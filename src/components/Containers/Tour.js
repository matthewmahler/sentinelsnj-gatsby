import React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Media = () => {
  return <Container id="tour">tour here later</Container>;
};

export default Media;
